# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20_2<-bind_rows(Ar20,Ba20,Fu20)
View(file_contents)
View(file_contents)
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20_2<-bind_rows(file_contents$Ar20,file_contents$Ba20,file_contents$Fu20)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in file_contents - increase reproducibility
Dat <- set_names(file_contents, names) # sets the names for the data frames in file_contents
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Fu10
Dat$Ba10
library(dplyr)
library(vegan)
library(tidyverse)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Ba10
Dat$Fu10 <- Dat$Fu10 %>%
En<-bind_rows(file_contents$En10,file_contents$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
Dat$Ba10
Dat$Fu10
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,Guild,Confidence.Ranking)
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
# _2 is present just to make sure that I got the write datesets labled through the string names
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
# _2 is present just to make sure that I got the write datesets labled through the string names
View(D10)
View(D10)
Dat$Fu20 <- Dat$Fu20 %>%
select(-Trophic.Mode,Guild,Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Ba10
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,Guild,Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
library(dplyr)
library(vegan)
library(tidyverse)
Dat$Ba10
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Ba10
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
# _2 is present just to make sure that I got the write datesets labled through the string names
Dat <- Dat %>%
select(Fu10,-Trophic.Mode,-Guild,-Confidence.Ranking) %>%
select(Fu20,-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat <- Dat %>%
select(Fu10,-Trophic.Mode,-Guild,-Confidence.Ranking) %>%
select(Fu20,-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat <- Dat %>%
select.list(Fu10,-Trophic.Mode,-Guild,-Confidence.Ranking) %>%
select(Fu20,-Trophic.Mode,-Guild,-Confidence.Ranking)
head(En)
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking) %>%
rename(Clearcut=(CLF))
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking) %>%
rename(Clearcut=(CLF))
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking) %>%
rename(Clearcut=(CLF))
head(En)
head(En)
Dat$En <- Dat$En %>%
rename(Clearcut=(CLF))
Dat$En <- Dat$En %>%
rename( Clearcut=(CLF))
head(En)
head(En20)
head(En10)
head(En20)
head(En$10)
head(Dat$En10)
Dat$En20 <- Dat$En20 %>%
rename( Clearcut=(CLF))
head(Dat$En20)
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
# _2 is present just to make sure that I got the write datesets labled through the string names
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking) %>%
Dat$Fu20 <- Dat$Fu20 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu10 <- Dat$Fu10 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>%
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
head(En)
head(Dat$En10)
head(Dat$En20)
Dat$En20 <- Dat$En20 %>%
rename( Clearcut=(CLF))
head(Dat$En20)
head(Dat$En10)
head(Dat$En20)
head(Dat$En10)
head(Dat$En20)
rename( Clearcut=(CLF), Salvage=(Salv_log)
Dat$En20 <- Dat$En20 %>%
rename( Clearcut=(CLF), Salvage=(Salv_log)
Dat$En20 <- Dat$En20 %>%
Dat$En20 <- Dat$En20 %>%
rename(Clearcut=(CLF)) %>%
rename(Salvage=(Salv_log))
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$En20 <- Dat$En20 %>%
rename(Clearcut=(CLF)) %>%
rename(Salvage=(Salv_log))
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
# _2 is present just to make sure that I got the write datesets labled through the string names
View(En)
View(En)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$En20 <- Dat$En20 %>%
rename(Clearcut=(CLF),
Salvage=(Salv_log))
Dat$En20 <- Dat$En20 %>%
rename(Clearcut=(CLF),
Salvage=(Salv_log),
Organic.Carbon=(Organic.Carbon_prop),
Exc.Magnesium=(Exc..Magnesium),
pH.CaCl12.=(pH.Level..CaCl12.))
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$En20 <- Dat$En20 %>%
rename(Clearcut=(CLF),
Salvage=(Salv_log),
Organic.Carbon=(Organic.Carbon_prop),
Exc.Magnesium=(Exc..Magnesium),
pH.CaCl12.=(pH.Level..CaCl12.))
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$En20 <- Dat$En20 %>%
rename(Clearcut=(CLF),
Salvage=(Salv_log),
Organic.Carbon=(Organic.Carbon_prop),
Exc.Magnesium=(Exc..Magnesium),
pH.CaCl12.=(pH.Level..CaCl12.))
head(Dat$En20)
Dat$En20 <- Dat$En20 %>%
rename(Clearcut=(CLF),
Salvage=(Salv_log),
Organic.Carbon=(Organic.Carbon_prop),
Exc.Magnesium=(Exc..Magnesium),
pH.CaCl2.=(pH.Level..CaCl2.))
View(En)
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
# _2 is present just to make sure that I got the write datesets labled through the string names
View(En)
Dat$Fu10 <- Dat$Fu10 %>% Removes extras from Fungi
View(Dat)
Dat$Ar10
Dat$Ar10 <- Dat$En20 %>%
rename(Phylum=(Phylum),
Class=(Class),
Order=(Order),
Family=(Family))
Dat$Ar10 <- Dat$En20 %>%
rename(Phylum=(phylum),
Class=(class),
Order=(order),
Family=(family))
Dat$Ar10 <- Dat$Ar10 %>%
rename(Phylum=(phylum),
Class=(class),
Order=(order),
Family=(family))
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# mutate and En20
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines but all for at depth 10
# Trophic mode, guild, confidence rating can likely be removed from fungi data sets
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20)
# _2 is present just to make sure that I got the write datesets labled through the string names
View(D10)
View(D20)
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# It may be possible to combine
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines all for at depth 10
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20) # Combines all microbes for for depth of 20
Dat$Fu20 <- Dat$Fu20 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# It may be possible to combine
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines all for at depth 10
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20) # Combines all microbes for for depth of 20
View(D20)
View(Dat)
View(Dat)
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
library(dplyr)
library(vegan)
library(tidyverse)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Ba10 <- Dat$Ba10 %>%
rename(Cluster=(cluster))
Dat$Ar10 <- Dat$Ar10 %>% # Fixes capitalization
rename(Phyla=(phylum),
Class=(class),
Order=(order),
Family=(family))
Dat$En20 <- Dat$En20 %>% # Makes so that when both Enviro tables are combined names match
rename(Clearcut=(CLF),
Salvage=(Salv_log),
Organic.Carbon=(Organic.Carbon_prop),
Exc.Magnesium=(Exc..Magnesium),
pH.CaCl2.=(pH.Level..CaCl2.))
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# It may be possible to combine
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines all for at depth 10
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20) # Combines all microbes for for depth of 20
View(D10)
View(D20)
View(Dat)
View(En)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Ba10 <- Dat$Ba10 %>%
rename(Cluster=(cluster))
Dat$Ar10 <- Dat$Ar10 %>% # Fixes capitalization
rename(Phyla=(phylum),
Class=(class),
Order=(order),
Family=(family))
Dat$En10 <- Dat$En10 %>%
rename(Archaea=(Archea_010cm),
Bacteria=(Bacteria_010cm),
Fungi=(Fungi_010cm))
Dat$En10
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Ba10 <- Dat$Ba10 %>%
rename(Cluster=(cluster))
Dat$Ar10 <- Dat$Ar10 %>% # Fixes capitalization
rename(Phyla=(phylum),
Class=(class),
Order=(order),
Family=(family))
Dat$En10 <- Dat$En10 %>%
rename(Archaea=(Archaea_010cm),
Bacteria=(Bacteria_010cm),
Fungi=(Fungi_010cm))
Dat$En20 <- Dat$En20 %>% # Makes so that when both Enviro tables are combined names match
rename(Clearcut=(CLF),
Salvage=(Salv_log),
Organic.Carbon=(Organic.Carbon_prop),
Exc.Magnesium=(Exc..Magnesium),
pH.CaCl2.=(pH.Level..CaCl2.),
Archaea=(Archaea_020cm),
Bacteria=(Bacteria_020cm),
Fungi=(Fungi_020cm))
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset, but some manipulation to get all the column names correct between the two still needs to happen.
# It may be possible to combine
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines all for at depth 10
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20) # Combines all microbes for for depth of 20
tD10 <- D10
tD10 <- D10
t(tOTU[1:3,1:3])
t(tD10[1:3,1:3])
(tD10[1:3,1:3])
tD10 <- t(tD10)
View(tD10)
View(En)
View(tD10)
tD10 <- tD10 %>%
(tD10[1:3,1:3])
library(dplyr)
library(vegan)
library(tidyverse)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines all for at depth 10
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20) # Combines all microbes for for depth of 20
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines all for at depth 10
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20) # Combines all microbes for for depth of 20
library(dplyr)
library(vegan)
library(tidyverse)
temp <- list.files("./Data",pattern="*.csv", full.names = T) #creates an object that contains a string of file names with directory so they can be located
Dat <- list() # creates an empty vector used in the for loop
for (i in seq_along(temp)) { # creates a list (I dont know if this is the right term) of dataframes
Dat[[i]] <- read.csv(
file = temp[[i]]
)
}
names <- c("En10", "En20", "Ar10", "Ba10", "Fu10", "Ar20", "Ba20", "Fu20") #probably a better way to name the elements in Dat - increase reproducibility
Dat <- set_names(Dat, names) # sets the names for the data frames in Dat
Dat$Fu10 <- Dat$Fu10 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Fu20 <- Dat$Fu20 %>% #Removes extras from Fungi
select(-Trophic.Mode,-Guild,-Confidence.Ranking)
Dat$Ba10 <- Dat$Ba10 %>% # Fixes capitalization
rename(Cluster=(cluster))
Dat$Ar10 <- Dat$Ar10 %>% # Fixes capitalization
rename(Phyla=(phylum),
Class=(class),
Order=(order),
Family=(family))
Dat$En10 <- Dat$En10 %>% # Possibly unnecessary
rename(Archaea=(Archaea_010cm),
Bacteria=(Bacteria_010cm),
Fungi=(Fungi_010cm))
Dat$En20 <- Dat$En20 %>% # Makes so that when both Enviro tables are combined names match
rename(Clearcut=(CLF),
Salvage=(Salv_log),
Organic.Carbon=(Organic.Carbon_prop),
Exc.Magnesium=(Exc..Magnesium),
pH.CaCl2.=(pH.Level..CaCl2.),
Archaea=(Archaea_020cm),
Bacteria=(Bacteria_020cm),
Fungi=(Fungi_020cm))
En<-bind_rows(Dat$En10,Dat$En20) #Combines Enviro tables into one dataset
D10<-bind_rows(Dat$Ar10,Dat$Ba10,Dat$Fu10) # Combines all for at depth 10
D20<-bind_rows(Dat$Ar20,Dat$Ba20,Dat$Fu20) # Combines all microbes for for depth of 20
